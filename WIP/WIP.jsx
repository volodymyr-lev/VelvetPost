import React from "react";


export default function WIP({text}){
    return (
        <div style={{position:'absolute', top: 100, justifyContent:'center'}}>
            <p style={{color:'red'}}>{text}</p>
        </div>
    )
}